define mymain = lambda z:int. {
  define pi = 3.1415927;
  define area = lambda radius:float. radius*radius*pi;
  define intelligence_test = lambda x:float.{
    define pi = z;
    print(area(10.01));  
    print ("/n");
  };
  intelligence_test(51.01);
};//mymain

mymain(2);
